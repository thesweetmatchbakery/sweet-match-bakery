import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sweetmatchbakery.app',
  appName: 'Sweet Match Bakery',
  webDir: 'www'
};

export default config;
